# LLM provider: "bedrock" (default) or "anthropic" (local fallback)
LLM_PROVIDER=bedrock

# AWS Bedrock config (primary LLM access path)
AWS_REGION=us-east-1
AWS_PROFILE=default

# Direct Anthropic SDK (fallback only, used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_key_here
