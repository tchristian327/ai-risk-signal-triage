# App runtime dependencies (used by Streamlit Community Cloud).
# -e . installs the repo root as a package so `from src.schemas import Digest`
# works without sys.path hacks. Keep this file minimal — sentence-transformers
# pulls in PyTorch (~2GB) and will blow the Cloud install size limit.
-e .
streamlit
pydantic
